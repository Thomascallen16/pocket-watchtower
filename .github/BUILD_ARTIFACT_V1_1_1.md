# Pocket Watchtower v1.1.1 artifact validation

This marker exists only to trigger the release workflow on the validation branch. The Android APK is built from the same v1.1.1 application sources on `main`.
